// import React from "react";
import  ReactDOM from "react-dom/client"

const h1 = <h1>some text to append on window</h1>
const root = ReactDOM.createRoot(document.getElementById("root"))

root.render(h1)